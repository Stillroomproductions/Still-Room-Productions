# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Next.js app
nextjs-app/.next
nextjs-app/.env.local

nextjs-app/.next
nextjs-app/.env.local

.next

# Unused image files moved out of public/ during cleanup (49MB).
# They are NOT deleted — earlier versions of the film artwork may be in here.
# Recoverable from git history if ever needed; confirm originals are archived
# elsewhere before removing this folder for good.
_unused-images/
